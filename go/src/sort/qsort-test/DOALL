PROGS=(qsorttest qsortconctest)


for (p in $PROGS) {
    cmd1 = (go build --race $p.go)
    echo $cmd1
    if ($cmd1) {
        cmd2 = (./$p $1)
        echo $cmd2
        $cmd2
    } else {
        echo ERROR: Cmd $cmd1 failed
    }
}

